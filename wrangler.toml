name = "gloriafood-webhook-handler" # Puedes cambiar este nombre
main = "main.py"
compatibility_date = "2024-05-10"

# Define que SMARTPASSES_TOKEN será un "secret" (variable de entorno segura)
[[secrets]]
binding = "SMARTPASSES_TOKEN"